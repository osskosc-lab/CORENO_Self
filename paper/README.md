# Primary Paper

Expected primary source:

`CORENO_Self_Boundary_Update_Theory.pdf`

The paper is the conceptual source for Phase 0A. It should be added only with a provenance record (source filename, SHA256, date frozen, and the commit/PR that authorizes it).

Until that binary provenance freeze is completed, the repository scientific status remains **G — INSUFFICIENT_EVIDENCE** and the Phase 0A implementation must be interpreted only against the committed specification and claim firewall.

## Required paper amendment before a publication freeze

Replace ontological wording equivalent to:

> an invariant core does not exist

with the model-level commitment:

> **The model does not assume an invariant core.**

The experiment is not designed to prove the nonexistence of an invariant core in nature.
